<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">
    <changeSet id="add-cartId-column-orders" author="ilgiznigmatullin (generated)">
        <preConditions>
            <and>
                <tableExists tableName="orders" schemaName="${schemaName}"/>
                <dbms type="postgresql"/>
            </and>
            <and>
                <not>
                    <columnExists tableName="cart_id" columnName="username" schemaName="${schemaName}"/>
                </not>
            </and>
        </preConditions>

        <addColumn tableName="orders">
            <column name="cart_id" type="bigint">
                <constraints nullable="false"/>
            </column>
        </addColumn>

        <addForeignKeyConstraint baseTableName="carts" baseColumnNames="id" constraintName="fk_orders_carts_cartId"
                                 referencedTableSchemaName="${schemaName}"
                                 referencedTableName="carts"
                                 referencedColumnNames="id"/>
        <rollback>
            <dropColumn tableName="orders" schemaName="${schemaName}"/>
        </rollback>
    </changeSet>
</databaseChangeLog>